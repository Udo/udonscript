# UdonScript Geany Filetype

## Manual install
Copy `filetypes.UdonScript` to your Geany user config:

```bash
mkdir -p ~/.config/geany/filedefs
cp filetypes.UdonScript ~/.config/geany/filedefs/
```

Then restart Geany.
